apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  environment: dev
  building-block: build

namespace: chap14-review-cicd

resources:
  - Pipeline/namespace.yaml
  - Pipeline/quarkus-build-pi.yaml
  - PipelineRun/quarkus-build-pr.yaml