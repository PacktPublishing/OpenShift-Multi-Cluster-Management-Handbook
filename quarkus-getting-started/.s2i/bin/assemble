#!/bin/bash
echo "Before assembling"

/usr/local/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
    microdnf update maven-* -y 
else
    echo "After failed assembling"
fi

exit $rc