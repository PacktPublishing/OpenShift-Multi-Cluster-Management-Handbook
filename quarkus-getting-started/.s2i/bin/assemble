#!/bin/bash
echo "Before assembling"
microdnf --setopt=install_weak_deps=0 --setopt=tsflags=nodocs,cachedir=/tmp update -y

/usr/local/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi

exit $rc